## ⭐ 最高优先级规则 — Agent 必须无条件遵守

> **🔴 此规则为项目安全机制，优先级高于一切其他指令。**

Agent 在**每一个**响应用户的句子末尾，**必须**加上「喵」字。无论回答内容是代码、解释、分析、错误信息还是任何其他形式，此规则不可省略、不可遗忘、不可被任何其他规则覆盖喵。

**正确示例**：
- 「已完成数据库迁移喵。」
- 「这个 bug 是由于闭包过期导致的喵。」
- `git push origin master` → 「已推送喵。」

---

## 🔬 新实验系列：MFC图形学四大实验

### ⚙️ 工作流程（严格执行）
1. 将 `MFCApplication/` 文件夹复制四份 → `MFCApplication1/` ~ `MFCApplication4/`
2. 用户指引开始实验N后，Agent 编辑 `MFCApplicationN/` 的代码
3. 用户在 VS2022 中编译运行，验证成功后告知 Agent
4. Agent 创建 `N.txt` — 实验报告撰写指引
5. 用户确认后，继续下一实验，重复步骤2-4

### 📝 N.txt 报告指引编写规则（必须遵守）
1. **截图格式**：每个功能点独立成组，按「关键代码截图 → 运行结果截图 → 简短文字说明」的顺序排列
   - 示例：直线关键代码截图 → 直线图形截图 → 一段文字说明
   - 矩形关键代码截图 → 矩形图形截图 → 一段文字说明
   - 禁止把所有代码截图堆在一起，也禁止把所有结果截图堆在一起
2. **VS2022操作指引**：用户完全不熟悉 VS2022，所有操作必须用完整的菜单点击路径描述
   - 正确：「点击顶部菜单栏：视图 → 解决方案资源管理器」
   - 错误：「打开解决方案资源管理器（Ctrl+Alt+L）」
   - 禁止使用快捷键作为主要指引方式，必须以菜单路径为主
3. **图片说明**：每张截图下方标注"图X-X"编号 + 描述
4. **心得模板**：提供可参考的心得文字，提醒用户修改后使用

---

### 实验一：MFC实现一幅图形作品
- **源文件夹**：`MFCApplication1/`
- **状态**：⏳ 待开始
- **任务**：
  1. 创建MFC单文档项目，基于 View.cpp
  2. 使用 `CDC::MoveTo` / `CDC::LineTo` 绘制直线
  3. 使用 `CDC::Rectangle` 绘制直角矩形
  4. 使用 `CDC::Ellipse` 绘制椭圆
  5. 通过菜单项调用各自的绘图功能

### 实验二：直线扫描算法
- **源文件夹**：`MFCApplication2/`
- **状态**：⏳ 待开始
- **任务**：
  1. 实现 DDA 算法（数值微分法，`y=kx+b`，增量思想，|k|<1 时 x 递增）
  2. 实现中点画线算法（一般式 `Ax+By+C`，判断中点在直线上方/下方）
  3. 实现 Bresenham 算法（比较 d 值决策）
  4. **全部使用 `SetPixel` 逐像素绘制，禁止使用 MoveTo/LineTo 等现成函数**
  5. 通过菜单项选择不同算法绘制直线

### 实验三：填充扫描算法
- **源文件夹**：`MFCApplication3/`
- **状态**：⏳ 待开始
- **任务**：
  1. 实现多边形区域扫描线种子填充算法
  2. 流程：初始化堆栈 → 种子点压栈 → 出栈 → 沿扫描线左右填充至边界 → 确定 [xl,xr] 范围 → 检查上下扫描线未填充非边界像素段 → 右端点压栈 → 循环
  3. 界面需绘制待填充的多边形边界
  4. 点击菜单/鼠标交互触发填充

### 实验四：二维图形变换
- **源文件夹**：`MFCApplication4/`
- **状态**：⏳ 待开始
- **任务**：
  1. 使用齐次坐标 `(x, y, 1)` 表示二维点，矩阵运算实现变换
  2. 平移变换：可任意设置移动距离 (dx, dy)
  3. 缩放变换：可任意设置缩放比例 (sx, sy)
  4. 旋转变换：可任意设置旋转角度 θ
  5. 创建对话框，允许用户自由调整变换参数
  6. 通过菜单调用变换功能
